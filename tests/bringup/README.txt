 - To run bring-up tests, replace the main.c file in the MC project with the file mc/tests/arena_bringup/bringup_main.c 
 - bringup_tests.h will contain function declerations for all bringup related tests.
 - sys_is_master_core() API will work in tests (both initialization and test).
 